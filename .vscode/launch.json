{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Hermes application",
            "cwd": "${workspaceFolder}/app",
            "type": "reactnativedirect",
            "request": "attach"
        }
    ]
}